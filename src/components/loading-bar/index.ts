/**
 * Barrel for <sce-loading-bar>. Importing this file registers the custom
 * element as a side effect. Consumers who want the React adapter should
 * import from `stock-elements/loading-bar/react` instead.
 */
export { SceLoadingBar } from './loading-bar.js';
export type { LoadingBarMode, LoadingBarProps } from './types.js';
