/**
 * Barrel for <sce-loading-grid>. Importing this file registers the
 * custom element as a side effect. Consumers who want the React adapter
 * should import from `stock-elements/loading-grid/react` instead.
 */
export { SceLoadingGrid } from './loading-grid.js';
export type { LoadingGridMode, LoadingGridProps } from './types.js';
